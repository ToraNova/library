env-pathing scripts
chia jason 2018
mailto: chia_jason96@live.com

The scripts here allow the user to easily inject paths to the environment variable of him/herself
simply call the .sh scripts here with the desired paths to inject/snipe.

One can also call hrpath.sh to view the path in human-readable form.
